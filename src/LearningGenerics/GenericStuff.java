package LearningGenerics;

public class GenericStuff {

    /**
     * Generics can be used with classes, methods.
     * It allows error that will appear in runtime to be caught in compile time.
     * Generics ensure type safety.
     * Generics make use of primitive type and not reference type. i.e Long instead of long, Double instead of double.
     * String by default is a reference type.
     *
     * Stack operates LIFO, Last In, First Out.
     * isEmpty(), isFull(), push, pop, peek, size()
     */
    public static void main(String[] args) {
        int[] array = new int[5];
    }
}
